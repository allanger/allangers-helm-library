{
  "additionalProperties": false,
  "properties": {
    "global": {
      "description": "Global values are values that can be accessed from any chart or subchart by exactly the same name.",
      "title": "global",
      "type": "object"
    },
    "helm-library": {
      "properties": {
        "global": {
          "description": "Global values are values that can be accessed from any chart or subchart by exactly the same name.",
          "title": "global",
          "type": "object"
        },
        "test": {
          "default": "test",
          "description": "helm-docs description here",
          "title": "test",
          "type": "string"
        }
      },
      "description": "A Helm library to be reused by allanger's charts. It's supposed to become an alternative for k8s-at-home",
      "title": "helm-library",
      "type": "object"
    },
    "lala": {
      "default": "lala",
      "title": "lala",
      "type": "string"
    },
    "test": {
      "items": {
        "anyOf": [
          {
            "type": "string"
          },
          {
            "type": "string"
          }
        ]
      },
      "title": "test",
      "type": "array"
    }
  },
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": [
    "test",
    "lala"
  ]
}
